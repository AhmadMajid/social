<div class="vibration hover d-flex py-3 px-3">
  <div class="flex-shrink-0">
    <% if current_user.avatar.present? %>
      <%= image_tag current_user.avatar, size: "48x48", class: "rounded-circle" %>
    <% else %>
      <%= image_tag "user.png", size: "48x48", class: "rounded-circle" %>
    <% end %>
  </div>
  <div class="flex-grow-1 ms-3">
    <%= form_with model: Vibration.new, url: form_submit_path, method: :post, class: "w-100", data: { controller: 'vibration-form' } do |f| %>
      <%= f.text_area :body, placeholder: "What's happening?", class: "form-control w-100 vibration-input mb-4" %>
      <div class="d-flex justify-content-end">
        <%= f.submit button_label, class: "btn btn-lg btn-social rounded-pill" %>
      </div>
    <% end %>
  </div>
</div>
